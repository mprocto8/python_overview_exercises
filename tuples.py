# Tuples are surrounded by parentheses and are immutable
numbers = (1, 2, 3, 3, 3, 3)
print(numbers)

# Count function returns number of times an entry is found in the tuple
print(numbers.count(3))

# Index function returns location of an entry's first occurence in a list or tuple
print(numbers.index(3))